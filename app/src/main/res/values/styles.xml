<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Typography scale (Poppins Medium / Bold shipped in res/font) -->

    <!-- Small uppercase label, e.g. section markers -->
    <style name="AppText.Overline" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/bold</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/text_muted</item>
        <item name="android:letterSpacing">0.14</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Screen heading -->
    <style name="AppText.Headline" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/bold</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Card / item title -->
    <style name="AppText.Title" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/bold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Supporting copy -->
    <style name="AppText.Body" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/font</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Secondary caption -->
    <style name="AppText.Caption" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/font</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/text_muted</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Buttons -->
    <style name="AppText.Button" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:letterSpacing">0.01</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- ProgressBar Style -->
    <style name="CustomProgressBarStyle" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@color/primary</item>
        <item name="android:indeterminateTintMode">src_in</item>
    </style>

    <!-- Alert Dialog Style -->
    <style name="AlertDialogStyle" parent="Theme.MaterialComponents.Dialog">
        <item name="android:windowBackground">@drawable/bg_card_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:windowMinWidthMinor">85%</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
